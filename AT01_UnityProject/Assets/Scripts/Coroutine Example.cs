using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

   private IEnumerator WaitAndPrint(float _withTime)
   (
    Debug.Log("First Message,so time has passed);
    yield return new WaitForSeconds(_waitTime);
    Debug.Log("Next Message, " + _waitTime + " seconds have passed");
    yield return new waitForSeconds(_waitTime + " seconds have passed");
    Debug.Log("Next Message, " + (_waitTime + if) + " seconds have passed")
   )
}
